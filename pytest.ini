[pytest]
addopts = -ra
# Only look for tests under the test/ folder
testpaths = test
# Enable asyncio support automatically (pytest-asyncio)
asyncio_mode = auto
# Optional: show logger output in test runs
log_cli = true
log_cli_level = INFO
